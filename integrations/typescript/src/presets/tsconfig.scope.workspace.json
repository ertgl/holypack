{
  "extends": "./tsconfig.partial.base.json",
  "compilerOptions": {
    "baseUrl": "${configDir}",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "rootDir": "${configDir}"
  },
  "include": [
    "${configDir}/**/*"
  ],
  "exclude": [
    "${configDir}/.git/**/*",
    "${configDir}/.yarn/**/*",
    "${configDir}/node_modules/**/*"
  ]
}
