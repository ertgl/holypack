{
  "name": "@holypack/e2e-integration-custom-basic",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "yarn clean-cache && yarn clean-coverage",
    "clean-cache": "rm -fr .cache",
    "clean-coverage": "rm -fr coverage",
    "jest": "yarn cmd:jest",
    "test": "yarn coverage:clean && yarn jest"
  },
  "devDependencies": {
    "@holypack/babel-preset": "*",
    "@holypack/core": "*",
    "@holypack/integration-babel": "*",
    "@holypack/integration-jest": "*",
    "@holypack/jest-preset": "*",
    "holypack": "*",
    "jest": "^29.7.0"
  },
  "peerDependencies": {
    "@holypack/core": "*"
  }
}
