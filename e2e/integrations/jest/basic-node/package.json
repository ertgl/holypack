{
  "name": "@holypack/e2e-integration-jest-basic-node",
  "private": true,
  "type": "module",
  "scripts": {
    "cache:clean": "rm -fr .cache",
    "clean": "yarn cache:clean && yarn coverage:clean",
    "coverage:clean": "rm -fr coverage",
    "jest": "yarn cmd:jest",
    "test": "yarn coverage:clean && yarn jest"
  },
  "devDependencies": {
    "@holypack/babel-preset": "*",
    "@holypack/core": "*",
    "@holypack/integration-babel": "*",
    "@holypack/integration-jest": "*",
    "@holypack/integration-typescript": "*",
    "@holypack/jest-preset": "*",
    "holypack": "*",
    "jest": "^30.0.4"
  }
}
